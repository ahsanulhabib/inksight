# --- Virtual Environment ---
# Ignore the virtual environment folder to avoid committing project-specific Python interpreters and packages.
.venv/
venv/

# --- Data and User-Uploaded Files ---
# Per your request, ignore all content inside the data folder.
# This prevents committing potentially large, sensitive, or copyrighted documents.
data/*

# The ! negates the previous rule for this specific file. This is a common
# pattern to keep the 'data' directory in the repository as a placeholder,
# even if it's empty. You will need to create an empty file named .gitkeep
# inside the data folder for this to work.
!data/.gitkeep

# --- Python Cache and Artifacts ---
# Ignore Python's bytecode cache, which is generated automatically.
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Package Caches ---
# Ignore local caches from package managers like uv.
.uv_cache/

# --- IDE and Editor Configuration ---
# Ignore user-specific settings from popular editors like VS Code and PyCharm.
.vscode/
.idea/

# --- Operating System Files ---
# Ignore metadata files generated by macOS and Windows.
.DS_Store
Thumbs.db

# --- Generated Files ---
# Igno